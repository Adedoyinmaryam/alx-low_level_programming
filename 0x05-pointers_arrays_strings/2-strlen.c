#include "main.h"
#include <stdio.h>
#include <string.h>
/**
 * _strlen - gets character string
 *
 * @s: character to getstring
 * Return: returns length
 */
int _strlen(char *s)
{
	int len;

	s = {};
	len = _strlen(s);
	printf("_strlen(s) :  %d\n", len);
	return (0);
}
